# Media Ratings Platform - Intermediate Configuration
# Database Configuration
db.url=jdbc:postgresql://localhost:5432/mrp_db
db.username=mrp_user
db.password=mrp_password
db.driver=org.postgresql.Driver

# Server Configuration
server.port=8080
server.host=localhost

# TODO: Add proper configuration management in final submission
# TODO: Implement environment-specific configurations
# TODO: Add connection pool settings without HikariCP (removed from dependencies)
